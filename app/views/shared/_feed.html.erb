<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render partial: "layouts/partials/micropost",
               collection: @feed_items, as: :item %>
  </ol>
  <%= paginate @feed_items %>
<% end %>
